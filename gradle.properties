org.gradle.jvmargs=-Xmx2G

# --- Minecraft 26.1.2 -- sole active target for now (matches the actual test
# instance). Mojang removed obfuscation entirely starting with 26.1, so there
# is no Yarn-vs-Mojmap mapping choice anymore -- the client jar ships with
# real class/method names built in. Yarn has zero coverage for this version
# (confirmed against Fabric's own maven metadata: no 26.x entries at all).
# Known-good set verified 2026-08-28 against Fabric's live maven metadata.
minecraft_version=26.1.2
loader_version=0.18.4
fabric_api_version=0.148.0+26.1.2
loom_version=1.15.5

# MC 26.1.2 requires Java 25 (confirmed via Mojang's version manifest).
java_version=25

mod_version=1.0.0
maven_group=com.ardor
archives_base_name=ardor
